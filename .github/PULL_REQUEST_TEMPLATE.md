<!-- Keep what applies; delete the rest. -->

## What

## Verification
<!-- Commands run and their results, or the tests that cover this. -->

## Documentation (only if this PR touches docs or user-visible behaviour)

- Scope of any claims added/changed: `SysML v2 / KerML` / `sysml-rs implementation` / `sysml-rs tooling` / `OMG API subset` / `Experimental / partial support`
- Evidence: language claims cite an OMG source; behavioural claims show a real command run or a contract test (see `docs/documentation-policy.md`).
- If user-visible behaviour changed: are the portal pages (`website/`) and generated references still accurate? (`cd website && npm run gen-check`)
